# Pain Hunter profile overrides (optional).
# Global keys are read from the root .env — no need to repeat them here.

AGENT_PORT=8100
# OPENROUTER_API_KEY=sk-or-...  # override root key for this profile only
