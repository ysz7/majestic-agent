services:
  majestic:
    build: .
    env_file: .env
    environment:
      MAJESTIC_HOME: /data
    volumes:
      - ${MAJESTIC_HOST_HOME:-~/.majestic-agent}:/data
    ports:
      - "8080:8080"
    restart: unless-stopped
